# ピッタリ小銭パズル — 現行仕様書

更新日：2026-09-08

## 1. 概要と仕様の基準

手持ちのお金でシュールな商品を購入し、財布の硬貨枚数と容量を管理しながら購入合計金額を伸ばす、思考型カジュアルパズルゲーム。スマートフォンの縦画面を中心に、PC・横画面にも対応するブラウザゲームとする。

本書は現行実装の仕様をまとめたものであり、過去の試作ルールを追記で上書きする構成は採用しない。

**初期所持金・初期容量・容量ボーナスの調整値は、常に [wallet-config.js](wallet-config.js) を唯一の正とする。** 本書・README・テスト・分析結果に記載された固定値と異なる場合も、これらの設定については `wallet-config.js` を優先する。随時調整する値の重複管理を避けるため、本書に現在値は転記しない。

設定で変更できないゲームルールは `game.js`、操作・演出は `app.js`、画面構造とレイアウトは `index.html`・`style.css` に実装する。

## 2. 実行環境とファイル構成

ビルドやパッケージのインストールは不要。`index.html` をブラウザで直接開くか、静的HTTPサーバーで配信する。バックエンドは使用しない。

| ファイル | 役割 |
| --- | --- |
| `wallet-config.js` | 初期財布・初期容量・容量ボーナスの設定 |
| `products.js` | 商品マスター（シュール50種類・日常50種類） |
| `game.js` | 描画から独立したゲームルール。`globalThis.CoinGame` を公開 |
| `app.js` | 入力、画面更新、効果音、会計演出、最高額保存 |
| `index.html` | ゲーム画面、遊び方・結果ダイアログ |
| `style.css` | レイアウト、装飾、アニメーション |
| `tests/` | ロジック、画面操作、画面サイズの検証 |
| `analysis/` | バランス分析用スクリプトと保存済み結果 |

スクリプトは `wallet-config.js` → `products.js` → `game.js` → `app.js` の順に読み込む。商品画像は絵文字による仮イラストで、見た目は端末に依存する。Google Fontsを利用できない環境ではシステムフォントで表示する。

## 3. 財布の設定と初期化

設定ファイルは `globalThis.WALLET_CONFIG` を定義するJavaScript形式とし、直接HTMLを開く場合も読み込めるようにする。

| 設定キー | 内容・制約 |
| --- | --- |
| `coins` | 500・100・50・10・5・1円の初期枚数。各値は0以上の安全な整数 |
| `notes` | 1,000・5,000・10,000円の初期枚数。各値は0以上の安全な整数、または無制限を表す `null` |
| `maxCoinsCapacity` | 初期硬貨容量。1以上の安全な整数で、初期硬貨の合計枚数以上 |
| `levelCapacityBonus` | PTによるレベルアップ1回の容量増加量。0以上の安全な整数 |
| `exactCapacityBonus` | お釣り0円の会計による容量増加量。0以上の安全な整数 |

すべての対応額面と容量ボーナスのキーを必須とし、硬貨・紙幣に未対応の額面を指定した場合もエラーにする。0枚は所持なし、ボーナス0は容量増加なしを意味する。紙幣は硬貨容量に含めない。

初期状態は、設定の所持枚数・容量をコピーし、トレイと購入履歴は空、購入総額は0、ゲーム終了フラグは偽、内部レベルは1、保有PTは0、必要PTは5とする。容量ボーナスもゲーム状態へコピーする。不正な設定の場合は設定エラーを画面に表示し、ゲームを開始しない。

設定ファイルの変更は、保存後にページを再読み込みして反映する。「もう一度お買いもの」は読み込み済み設定から初期化するため、ファイル変更の反映にはページ再読み込みが必要。進行中の財布や購入履歴は引き継がない。

## 4. 基本のゲームサイクル

1. 商品と生成した価格を提示する。
2. 硬貨をタップ・上スワイプ、紙幣をタップし、1枚ずつ支払いトレイへ投入する。
3. 価格以上の金額を用意して「お会計」を押す。
4. お釣り、PT、容量ボーナス、摩耗、ゲームオーバーを計算して状態を確定する。
5. 商品退場・お釣り返却・ポイント・容量変化の演出を表示する。
6. ゲームオーバーなら結果を表示し、それ以外は次の商品へ進む。容量が増えた会計も次の商品へ進む。

トレイのお金は1枚ごとにタップして返却でき、「全返却」で投入をすべて取り消せる。有限の所持金は投入時に減らし、返却時に戻す。無制限の紙幣は枚数を減らさない。所持0枚の額面は投入できない。

金額不足の会計では状態を変更しない。演出中やゲームオーバー後は投入・返却・再会計を受け付けない。

## 5. お釣りと会計予測

### 5.1 お釣りの内訳

お釣り総額は `投入金額 − 商品価格` とする。千円単位は10,000・5,000・1,000円札の順に返し、1,000円未満の端数は500・100・50・10・5・1円硬貨の貪欲法で最少枚数にする。お釣りの硬貨は最大15枚。

有限設定の紙幣は額面別にお釣りを加算する。無制限の額面は `null` のまま保持する。

例：9,030円の商品に10,000円札と50円玉を出すと、お釣りは1,020円。1,000円札1枚と10円玉2枚を返す。硬貨を1枚投入し2枚受け取るため、会計による硬貨の純増は1枚となる。

### 5.2 予測と確定の共通化

`previewPayment()` は状態を変更せず、会計結果を計算する。`pay()` は同じ予測結果を使って所持金・容量・PT・購入履歴・終了状態を更新する。

- 硬貨の純増減：`お釣り硬貨枚数 − 投入硬貨枚数`
- 会計後の硬貨枚数：`投入後に財布に残っている硬貨枚数 ＋ お釣り硬貨枚数`

支払い可能なとき、純増減、会計後の枚数、お釣り総額と硬貨枚数、紙幣のお釣り額、獲得PTや容量変化の予測を表示する。容量超過または容量0以下になる場合は警告する。

追加・1枚返却・全返却に合わせて予測を更新する。金額不足時は不足額を表示し、会計結果の予測は表示しない。未投入・全返却後・会計演出中も増減予測を表示しない。

## 6. PT・容量ボーナス・摩耗

必要PTは **5で固定**。容量が増えない会計の摩耗は **1枚で固定**。この2項目は `wallet-config.js` では変更できない。

| 会計条件 | PT処理 | 容量ボーナス |
| --- | --- | --- |
| お釣り総額が0円 | PTを加算せず、既存PTを保持 | `exactCapacityBonus` |
| お釣り総額が1円以上 | `max(0, 投入硬貨枚数 − お釣り硬貨枚数)` を加算 | 加算後5PTごとに `levelCapacityBonus` |

5PTごとに内部レベルを1上げ、1回につき5PTを消費して容量ボーナスを付与する。余剰PTは即時に次の蓄積へ繰り越し、1会計で複数回レベルアップできる。必要PTは以後も5のまま。例：3PT＋6PTでは1回レベルアップして4PTが残る。ピッタリ支払いではPTレベルアップを行わない。

ピッタリの判定は紙幣を含むお釣り総額が0円であること。財布に硬貨が残っていても対象となり、硬貨が0枚になっただけではボーナスは発生しない。紙幣だけのお釣りがある会計もピッタリ扱いにしない。

処理順序は以下とする。

1. PT加算・レベルアップ・PT繰り越しと、該当する容量ボーナスを計算する。
2. ボーナスによって容量が実際に増えた会計では摩耗を免除する。それ以外は容量を1枚減らす。
3. 増減後の容量でゲームオーバーを判定する。

ボーナス設定が0の場合は、ピッタリやレベルアップでも容量が増えないため摩耗する。摩耗に下限はなく、容量10枚未満でも減る。投入・返却・予測だけではPTや摩耗は発生しない。内部の `wearTurns` は完了会計ごとに増えるカウンターであり、摩耗の発生間隔を決める値ではない。

## 7. ゲームオーバー・スコア・保存

会計後に次のいずれかを満たすとゲームオーバーになる。

- 増減後の容量が0以下。硬貨が0枚でも終了する。
- 会計後の硬貨枚数が増減後の容量を超える。容量と同数なら継続する。

終了の原因になった会計も購入履歴・購入数・購入合計金額に含める。購入合計金額をスコアとし、最高額を `localStorage` の `kozeni-best` に保存する。保存機能が使えなくてもゲームは動作する。

結果ダイアログはレシート形式で全購入品、総額、購入数を表示する。「もう一度お買いもの」で新しいゲームを開始する。途中状態の保存・再開はなく、ページ再読み込みで最高額以外のゲーム進行をリセットする。

有限紙幣の設定で所持金が足りなくなった場合、資金不足専用のゲームオーバー判定はない。ページの再読み込みで初期状態に戻せる。

## 8. 商品と価格生成

### 8.1 商品一覧と出品順

商品一覧は [products.js](products.js) の `globalThis.PRODUCT_CATALOG` を唯一の正とし、ゲームロジックから分離して管理する。HTMLを直接開く動作を維持するため、データ専用JavaScript形式を採用する。

商品は計100種類。既存6商品を含むシュールな商品50種類と、食品・日用品・文具・家電など実際にありそうな普通の商品50種類で構成する。分類値はそれぞれ `シュール`・`日常` とする。

各商品は重複しない `name`、正の安全な整数の基準価格 `price`（価格生成時に安全な整数の範囲を超えない値）、`emoji`、`category`、`description`、6桁の16進色 `color` を持つ。空欄・不正値・名前の重複は読み込み時にエラーにする。編集後はページを再読み込みする。実際の販売価格は基準価格の千円単位部分に1〜1,000円を加えた範囲となる。

最初の商品はリスト先頭のアイス。以降は全100商品をバッグに入れて並べ替え、一巡ごとに補充する。50%ずつはマスターの構成比で、短いプレイ中の出品比率を保証するものではない。現在の実装は `sort(() => Math.random() - .5)` による並べ替えで、全順列の均等抽選は保証しない。バッグの次の商品が直前と同じ場合は別の商品と入れ替え、連続を避ける。
初回・リスタートを含む全出品で価格を生成する。基準商品価格の千円単位部分に、抽選したリスクに対応する1〜1,000円の候補を加える。購入済み商品の価格は後から変更しない。

### 8.2 リスクと難易度

リスクは `ceil(価格 / 1000) × 1000` を払ったときのお釣り硬貨枚数で定義する。プレイヤーが実際に選んだ支払い方や所持紙幣の制限とは独立した価格の指標とする。

| リスクレベル | お釣り硬貨枚数 |
| --- | --- |
| 1 | 0〜3枚 |
| 2 | 4〜6枚 |
| 3 | 7〜9枚 |
| 4 | 10〜12枚 |
| 5 | 13〜15枚 |

例えば999円はお釣り1枚でレベル1、1円は15枚でレベル5となる。

| 出品する品目 | 購入完了数 | レベル1 | レベル2 | レベル3 | レベル4 | レベル5 |
| --- | --- | --- | --- | --- | --- | --- |
| 1〜3品目 | 0〜2 | 60% | 30% | 10% | 0% | 0% |
| 4〜10品目 | 3〜9 | 10% | 30% | 40% | 20% | 0% |
| 11品目以降 | 10以上 | 5% | 15% | 30% | 35% | 15% |

先にリスクレベルを抽選し、そのレベルに一致する価格候補から等確率で選ぶ。価格のリスクレベルとPTで上がる内部レベルは別の概念とする。

## 9. 画面と操作表示

画面は上からヘッダー、スコア・戦利品、商品、支払いトレイ、財布を配置する。

- ヘッダー：ブランド、効果音切り替え、遊び方ボタン。
- スコア・戦利品：購入総額、購入数、最高額、直近16点の購入商品アイコン。
- 商品：絵文字、商品名、生成価格。カテゴリと商品説明はデータとして保持するが、画面では非表示。
- トレイ：「全返却」、1枚ずつ重ねたお金、投入額またはお釣り額、返却案内、会計予測。
- 財布上部：左半分に上から小銭枚数・5段階PTインジケータ・小銭上限、右半分に「お会計」。2つの枚数表示の右端と文字サイズを揃える。
- 財布下部：500・100・50・10・5・1円の順に硬貨を横並びで表示。その下に1,000・5,000・10,000円札を並べる。

硬貨の同額スタックは最大4枚の絵で表し、実際の枚数は数値で表示する。紙幣は無制限なら `∞`、有限なら所持枚数を表示し、0枚のボタンは無効化する。

PTインジケータは基準幅で高さ5pxの5分割表示とし、小銭枚数と上限の間の横線に見立てる。メイン画面にPTの数値ラベル・連続バー・摩耗カウントダウン・LV表記・小銭枚数バーは表示しない。商品の補助見出し・番号、トレイと財布の見出し、未投入時の指示文、下部の説明文も表示しない。

トレイでは同額でもまとめず、各1枚の角度・位置をずらす。枚数が多い場合は全体を縮小し、スクロールバーを使わず枠内に収める。

## 10. 演出・アクセシビリティ

商品は右から入り、会計時に左へ退場する。戦利品はCSSアニメーションで重なり・バウンドを表現する。

会計時は内部状態を先に確定し、表示用に会計前の財布状態を保持する。トレイにお釣りの紙幣と硬貨を並べ、約0.65秒後から財布の各額面の位置へ移動させる。返却演出が終わってから財布表示を確定後の値に切り替える。

お釣りがある会計でPTを獲得した場合は「〇枚へった！」を表示し、PTインジケータへ縮小移動させる。増分は左から25ms間隔で順に点灯する。5PT到達時は全点灯を60ms表示し、消灯して60ms後に上限アップ演出（800ms）、最後に余剰PTを左から25ms間隔で点灯する。同一会計で複数回レベルアップする場合は、5PT点灯・消灯・上限アップを1回ずつ繰り返し、最後に余剰PTを点灯する。各回の上限増加量は `levelCapacityBonus` とする。ピッタリではPT獲得表示の代わりに「ぴったり！」を表示する。

容量が会計前より増えた場合は上限の数字を拡大・バウンドさせる。容量変化は財布の演出・通知でも示す。ゲームオーバーでは財布の揺れとコイン飛散の後に結果ダイアログを開く。

効果音はWeb Audioで生成し、初期状態ではOFF。紙幣・硬貨投入、会計、お釣り、容量変化、終了に応じて鳴らす。

`prefers-reduced-motion` では商品・お釣り・ポイントの移動をフェード等に置き換え、容量数値の拡大を抑止する。ボタンに読み上げ用ラベル、PTに `progressbar`、会計予測や通知にライブ領域を提供する。

リスタートでは進行中の商品・ポイント・お釣り演出と通知を取り消す。非同期処理はゲーム状態の同一性を確認し、古い会計で新しいゲームを更新しない。アニメーション終了待ちには時間上限を設け、バックグラウンドタブでも会計ロックが残り続けないようにする。

## 11. レイアウトとセーフエリア

幅360pxを基準に、ゲーム枠の幅から文字・アイコン・操作部の大きさを決める。枠の最大幅は480px。同じ幅の画面では、縦長になっても要素の大きさは維持し、配置間隔を広げる。

枠の高さは、幅に比例する586〜650px相当の範囲で有効画面高に合わせる。それ以上の縦長画面では中央配置と木目背景で余白を埋める。横画面では枠幅を最大360pxとする。

`viewport-fit=cover` と上下左右の `safe-area-inset` を使い、ノッチ・ホームバー用の余白をゲーム枠の外側に確保する。有効高さに枠が収まらない場合はページをスクロールできる。

## 12. 検証と分析の扱い

`tests/test.html` をブラウザで開き、ロジックと画面操作を検証する。Node.jsがある環境では `node tests/run-logic.cjs` でロジック部分を実行できる。

主な検証対象は、お釣りの最少硬貨枚数、額面別の金額保存、有限紙幣の投入・返却、支払不足、PTと容量ボーナス、摩耗、容量境界、終了判定、リスク確率の切替、生成価格、予測と確定結果の一致、演出中の操作制限、リスタート時の演出キャンセルとする。

テストは専用の財布設定を使用し、バランス調整値から独立させる。実設定が初期状態に反映されることは別途検証する。テスト内の固定値を本番の初期設定と解釈しない。

`tests/layout.html` では320×512、360×576、390×624、390×844、375×667、844×390の表示を比較する。iPhoneの実際のセーフエリアはSafari実機で確認する。

`analysis/` の保存済み結果には旧設定や比較用ルールの結果を含むため、現行設定の実測値とは限らない。各結果の設定・READMEを確認する。シミュレーションはその会計で有利な支払いを選ぶ方針であり、将来を先読みする完全最適解や人間のプレイ成績を表すものではない。分析結果を初期設定やゲームルールの根拠として上書き適用しない。

### 表示更新

トレイ右下の返却案内は表示せず、「全返却」ボタンを配置する。文字サイズ・色は「投入額」と揃える。
ピッタリ会計ではPTインジケータをいったん消灯し、5PT点灯・消灯・上限アップを2周表示する。設定されたピッタリ容量ボーナスを2周に分けて反映し、最後に保持しているPTの点灯へ戻す。実際のPT加算・レベルアップは行わない。
リザルトでは小銭上限・枚数・PTを表示しない。お礼、品目リスト、合計金額、購入数（16px）、ぴったり回数、からっぽ回数（各14px）の順に並べる。回数は確定した会計だけを対象とし、重複成立はそれぞれ1回として数える。からっぽは小銭を持っていた状態から会計後に0枚になった場合を数える。リスタートで両方の回数を0に戻す。

## 小銭からっぽボーナス

会計前（投入した硬貨も含む）に小銭を持っていて、会計確定後に0枚になると `wallet-config.js` の `emptyCapacityBonus`（初期値5）分の容量を加算します。最初から0枚の会計や投入中だけ0枚になった場合は対象外です。ピッタリ・通常のPT報酬と重複し、容量が増えるため摩耗しません。

「からっぽ！！」を表示し、増加1枚につきインジケータ点灯→消灯→容量＋1の演出を1周行います。ピッタリとの重複時は、からっぽ5周→「ぴったり！」→ピッタリ2周の順です。保持PTは演出終了後に復元します。ピッタリでない場合は、からっぽ演出後に通常のPT獲得・レベルアップ・繰り越しを表示します。

## 上限突破とゴールドカード

会計のボーナス・摩耗を計算した上限が50枚以上になったら、終了判定より先に上限を20枚、小銭を0枚に戻し、ゴールドカードを1枚付与して続行します。50枚を超えて到達しても1会計につき1枚です。この20枚は上限突破後の固定値です。紙幣、購入履歴、総額、保有PT、レベル、達成回数は維持します。リセットによる小銭0枚はからっぽボーナスの対象にしません。

報酬演出の後に「上限突破！！」を表示し、財布に重なったゴールドカードと累計枚数を表示します。リスタートではカードも0枚に戻ります。

## イラストテーマ

`theme.css` が基本スタイルの後に読み込まれます。上からヘッダー・スコア・店員と商品吹き出し・支払いトレイ・財布の順序を維持。高さは基準幅360pxで700〜760px、収まらない端末はページを縦スクロールします。店内、硬貨6種類、架空紙幣3種類をローカル画像として使用し、トレイは生成画像、財布はCSSで質感を表現します。各画像は装飾であり、操作・価格・枚数・読み上げラベルはHTMLとゲーム状態が管理します。

吹き出しは角丸四角形＋左下のつの。紙幣は横縦比約2.05：1で一続きの収納ポケットに並び、額面と枚数は紙幣の上部に重ねて表示します。各紙幣の個別投入操作は維持します。

## トレイ固定・連続背景・店員の表情

トレイの高さは基準幅360pxで112pxに固定し、投入・返却・会計プレビュー・お釣り表示で変化させません。投入額欄はその直下に固定。店内素材の木目部分は表示せず、テーブルには共通画像を使用します。店員の顔は通常待機、会計成立後から次の商品表示までは笑顔。支払不足では変化せず、リスタートでも待機へ戻ります。
